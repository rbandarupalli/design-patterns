package designpatterns.behavioral.chainofresponsibility;

public class RootHandler extends Handler {

	@Override
	public void procureParts(String contextInfo) {
		// default request handler
		super.procureParts(contextInfo);
	}

	
}
